{
  "name": "gradients",
  "version": "1.0.0",
  "description": "WebGL gradient generator inspired by craft-gradients.artcreativecode.com",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "setup-analytics": "node scripts/setup-analytics.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "playwright": "^1.54.2",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.0"
  }
}
